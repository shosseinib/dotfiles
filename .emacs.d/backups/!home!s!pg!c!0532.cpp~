#include <iostream>
#iclude <cstdint> // for std::int_fast64_t
std::int_fast16_t pow(int base, int exp)
int main()
{
  std::int_fast64_t result {1};

  while (exp) {
    if (exp & 1) {
      result *= base;
      exp >>= 1;
      base *= base;
      }
    }
  return result;
}
